LVM_NAME=/dev/<%= @volumeGroupName %>/<%= @logicalVolumeName %>

mkdir -p /raid
mkdir -p /nfsexport/shared

if !(test -d /raid/shared); then
	if !(mountpoint -q /raid); then
		mount ${LVM_NAME} /raid
	fi
	mkdir -p /raid/shared
	umount /raid
fi

mount -a