check process socket-redis with pidfile /var/run/socket-redis.pid
	start program = "/etc/init.d/socket-redis start"
	stop program  = "/etc/init.d/socket-redis stop"
<% @socketPorts.each do |port| -%>
	if failed port <%= port %> with timeout 10 seconds then restart
<% end -%>
	if failed host localhost port <%= @statusPort %> protocol http with timeout 10 seconds then restart
	if 25 restarts within 50 cycles then timeout
