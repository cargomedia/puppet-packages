---
:backends:
  - json
:hierarchy:
  - fqdn/%{::fqdn_reversed}
  - datacenter/%{::datacenter}/%{::network}/%{::hostname}
  - datacenter/%{::datacenter}/%{::network}
  - datacenter/%{::datacenter}
  - defaults

:json:
  :datadir: <%= @hieraDataDir %>

:merge_behavior: deeper
