---
:backends:
  - json
:hierarchy:
  - datacenter/%{::datacenter}/%{::subnet}/%{::hostname}
  - datacenter/%{::datacenter}/%{::subnet}
  - datacenter/%{::datacenter}
  - defaults

:json:
  :datadir: <%= @hieraDataDir %>

:merge_behavior: deeper
