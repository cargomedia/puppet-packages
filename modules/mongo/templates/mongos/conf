# !!! generated by puppet !!!
# mongos.conf

logpath=<%= @log_dir %>/mongos.log

logappend=<%= @logappend %>

fork = <%= @fork %>

bind_ip=<%= @bind_ip %>

port = <%= @port %>

configdb = <%= @config_servers.join(",") %>

<% @options.each do |option| %>
<%= option %>
<% end %>
